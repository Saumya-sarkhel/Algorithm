Algo_Floyd(W,n)
{
    // W → Weight Matrix
	// n → Number of vertices
	for (i=1 to n)
	{
		for (j=1 to n)
		{
			for (k=1 to n)
			{
				if (W(j,i) + W(i,k) < W(j,k))
				{
					W(j,k) = W(j,i) + W(i,k)
				}
			}
		}
	}
}
